创建一个纯静态前端项目，使用 HTML + Tailwind CSS（通过 CDN 引入），不依赖 React/Vue 等框架。要求包括：

基础 HTML5 模板结构
通过 <script src> 引入 Tailwind CSS 最新版本
纯原生实现（无第三方库），代码干净且有注释
包含干净的目录结构建议（如 index.html、css/style.css、js/main.js）
响应式设计，适配移动端 